// Package config contains the loader and strongly typed model for services.yaml.
package config

// StackConfig is a placeholder for the future stack configuration model.
// It will represent project, environments, infrastructure groups and services.
type StackConfig struct{}

// LoadStackConfig is a placeholder that will load and parse services.yaml into StackConfig.
func LoadStackConfig(path string) (*StackConfig, error) {
	// Implementation will be added in the next development stages.
	return nil, nil
}
